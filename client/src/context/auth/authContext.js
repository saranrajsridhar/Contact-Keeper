import {createContext} from 'react';

const authContext=createContext();


export default authContext;



/*context is file where we use to share info from any component or state
to other component*/

//In this project i use an arch of context,reducer and state which is like hooks.